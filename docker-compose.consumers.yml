version: '2'

services:
    consumer:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Common/consumer.php

    dashboard_consumer:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Dashboard/consumer.php

    purchase_consumer:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Purchase/consumer.php

    stock_consume_once:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Stock/consume_once.php

    sales_process_manager:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Sales/process_manager.php

    consume_once:
        extends:
            file: docker/templates.yml
            service: php-cli
        command: php src/Common/consume_once.php
